<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.KisanSeva"
            android:usesCleartextTraffic="true"
            tools:targetApi="31">

        <!-- Splash Screen Activity -->
        <activity
                android:name=".SplashActivity"
                android:exported="true"
                android:theme="@style/Theme.KisanSeva.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
                android:name=".MainActivity"
                android:exported="false"
                android:configChanges="orientation|screenSize|keyboardHidden"
                android:windowSoftInputMode="adjustResize"/>

        <!-- Login Activity -->
        <activity
                android:name=".LoginActivity"
                android:exported="false"
                android:theme="@style/Theme.KisanSeva.NoActionBar"/>

        <!-- AI Pest Diagnosis Activity -->
        <activity
                android:name=".PestDiagnosisActivity"
                android:exported="false"
                android:label="AI Pest Diagnosis"/>

        <!-- Crop Calendar Activity -->
        <activity
                android:name=".CropCalendarActivity"
                android:exported="false"
                android:label="Crop Calendar"/>

        <!-- ROI Calculator Activity -->
        <activity
                android:name=".ROICalculatorActivity"
                android:exported="false"
                android:label="ROI Calculator"/>

        <!-- Knowledge Exchange Activity -->
        <activity
                android:name=".KnowledgeExchangeActivity"
                android:exported="false"
                android:label="Knowledge Exchange"/>

        <!-- Marketplace Activity -->
        <activity
                android:name=".MarketplaceActivity"
                android:exported="false"
                android:label="Marketplace"/>

        <!-- Product Details Activity -->
        <activity
                android:name=".ProductDetailsActivity"
                android:exported="false"
                android:label="Product Details"/>

        <!-- Language Settings Activity -->
        <activity
                android:name=".LanguageActivity"
                android:exported="false"
                android:label="Language Settings"/>

        <!-- File Provider for Camera -->
        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="${applicationId}.fileprovider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>
